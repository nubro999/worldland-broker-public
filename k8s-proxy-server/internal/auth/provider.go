// provider.go — placeholder for external identity-provider (Google /
// OIDC) integration. Intentionally empty: the shipped auth path is
// wallet-signature → JWT (jwt.go) plus session keys
// (internal/wallet); social login is a future option, not wired.
package auth
